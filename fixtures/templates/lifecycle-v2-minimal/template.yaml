apiVersion: statedd.stateport.io/v1alpha1
kind: Template
metadata:
  id: stateport.fixture.lifecycle-v2-minimal
  name: StatePort lifecycle v2 synthetic fixture
  version: 0.0.1
  description: Minimal invented fixture for lifecycle manifest v2 tests.
spec:
  domain: test-fixture
  lifecycle:
    - draft
  allowedActions:
    - name: read_state
      level: L0
  schemas: []
  agentContract:
    role: fixture-validator
    responsibilities:
      - Exercise lifecycle parsing
    forbiddenActions:
      - Treat this fixture as canonical content
