# Reference task service

This is an invented, public-safe StateBench calibration project. It is
deliberately incomplete: the visible test covers a small API helper while the
multi-stage feature spans typed models, JSON persistence, a service boundary,
a CLI surface and HTML rendering.

The fixture is production-ineligible and contains no private evaluator assets,
credentials, network integration or real user data.
